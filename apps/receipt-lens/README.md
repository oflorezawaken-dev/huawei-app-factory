# ReceiptLens

ReceiptLens is a privacy-first Android receipt scanner and expense organizer for
Huawei AppGallery. Receipt parsing and storage run on-device; the project does
not require cloud APIs, credentials, or API keys.

## Run locally

Open this directory in Android Studio, then run the `app` configuration on an
Android device or emulator.
