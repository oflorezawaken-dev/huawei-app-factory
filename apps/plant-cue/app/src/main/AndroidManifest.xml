<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Daily care reminder. Requested in context (after the first plant is saved), never at cold start. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required only by Huawei Petal Ads. Plant data never uses the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- No CAMERA permission: photos are taken through the system camera app (ACTION_IMAGE_CAPTURE),
         which needs no permission as long as the app does not declare CAMERA. -->

    <application
        android:name=".PlantCueApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Handles "Done" / "Snooze" actions from the reminder notification. -->
        <receiver
            android:name=".reminders.CareActionReceiver"
            android:exported="false" />

        <!-- Shares a private file with the system camera app when taking a plant photo. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager: disable the default initializer only if a custom one is added later. -->
    </application>

</manifest>
