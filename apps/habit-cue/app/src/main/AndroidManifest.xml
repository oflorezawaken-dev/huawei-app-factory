<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Daily habit reminder. Requested in context (after the first habit is saved), never at cold start. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required only by Huawei Petal Ads. Habit data never uses the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- No CAMERA, storage or location permission: this app has no use for any of them. -->

    <application
        android:name=".HabitCueApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- "Done all" action from the daily reminder notification. -->
        <receiver
            android:name=".reminders.HabitActionReceiver"
            android:exported="false" />

        <!-- WorkManager: disable the default initializer only if a custom one is added later. -->
    </application>

</manifest>
